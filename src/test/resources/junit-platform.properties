# ──────────────────────────────────────────────────────────────────────────────
# JUnit 5 Platform Configuration
# Task 4: Parallel Execution & Test Ordering
# ──────────────────────────────────────────────────────────────────────────────

# Enable parallel test execution
junit.jupiter.execution.parallel.enabled=true

# Default mode: run test methods within a class concurrently
junit.jupiter.execution.parallel.mode.default=concurrent

# Run test classes concurrently as well
junit.jupiter.execution.parallel.mode.classes.default=concurrent

# Use dynamic thread pool (scales with available CPU cores)
junit.jupiter.execution.parallel.config.strategy=dynamic
junit.jupiter.execution.parallel.config.dynamic.factor=1

# Default test method ordering (can be overridden per class with @TestMethodOrder)
junit.jupiter.testmethod.order.default=org.junit.jupiter.api.MethodOrderer$OrderAnnotation
